<form method="post" action="{{ path('a_p_i_delete', {'id': a_p_i.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a_p_i.id) }}">
    <button class="btn">Delete</button>
</form>
